use core
use s2.protocols.data.json
use s2.protocols.data.json.value

skill is_digit(ch) {
    out core.str.contains("0123456789", ch)
}

skill is_nonzero_digit(ch) {
    out core.str.contains("123456789", ch)
}

skill failure(index, message) {
    out json.Parse { index = index message = message }
}

skill parse_at(source, start) {
    @length = core.str.len(source)
    @index = start
    (index < length) {
        (core.str.at(source, index) == "-") { index = index + 1 }
    }
    (index == length) { out failure(index, "неполное число JSON") }

    @first = core.str.at(source, index)
    (first == "0") {
        index = index + 1
        (index < length) {
            (is_digit(core.str.at(source, index)) == yes) { out failure(index, "ведущий ноль в числе JSON") }
        }
    }
    (first == "0") { first = "" }
    (core.str.len(first) > 0) {
        (is_nonzero_digit(first) == no) { out failure(index, "ожидалось число JSON") }
        index = index + 1
        @active = yes
        drum (length + 1) {
            (active == yes) {
                (index < length) {
                    @digit = is_digit(core.str.at(source, index))
                    (digit == yes) { index = index + 1 }
                    (digit == no) { active = no }
                }
                (index == length) { active = no }
            }
        }
    }

    (index < length) {
        (core.str.at(source, index) == ".") {
            index = index + 1
            (index == length) { out failure(index, "в дробной части нет цифр") }
            (is_digit(core.str.at(source, index)) == no) { out failure(index, "в дробной части нет цифр") }
            @active = yes
            drum (length + 1) {
                (active == yes) {
                    (index < length) {
                        @digit = is_digit(core.str.at(source, index))
                        (digit == yes) { index = index + 1 }
                        (digit == no) { active = no }
                    }
                    (index == length) { active = no }
                }
            }
        }
    }

    (index < length) {
        @exponent = core.str.at(source, index)
        (exponent == "e") { index = index + 1 }
        (exponent == "E") { index = index + 1 }
        (exponent == "e") { exponent = "" }
        (exponent == "E") { exponent = "" }
        (core.str.len(exponent) == 0) {
            (index == length) { out failure(index, "в экспоненте нет цифр") }
            @sign = core.str.at(source, index)
            (sign == "+") { index = index + 1 }
            (sign == "-") { index = index + 1 }
            (index == length) { out failure(index, "в экспоненте нет цифр") }
            (is_digit(core.str.at(source, index)) == no) { out failure(index, "в экспоненте нет цифр") }
            @active = yes
            drum (length + 1) {
                (active == yes) {
                    (index < length) {
                        @digit = is_digit(core.str.at(source, index))
                        (digit == yes) { index = index + 1 }
                        (digit == no) { active = no }
                    }
                    (index == length) { active = no }
                }
            }
        }
    }

    out json.Parse { ok = yes value = value.number_value(core.str.slice(source, start, index)) index = index }
}

skill text_valid(subject) {
    @parsed = parse_at(subject, 0)
    (parsed.ok == yes) { out parsed.index == core.str.len(subject) }
    out no
}

skill integer_text_valid(subject) {
    (text_valid(subject) == no) { out no }
    (core.str.contains(subject, ".") == yes) { out no }
    (core.str.contains(core.str.lower(subject), "e") == yes) { out no }
    out yes
}
