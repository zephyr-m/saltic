use seed
use tests.fixtures.modules.nested.greeting

program() {
    seed.io.show(greeting.module_greeting("short"))
    seed.io.show(tests.fixtures.modules.nested.greeting.module_greeting("full"))
    seed.io.show(greeting.GreetingPrefix, "constant")
    seed.io.show(greeting.module_mood() == greeting.Mood.HAPPY)

    @short_box = greeting.Greeter { prefix = "short box: " }
    @full_box = tests.fixtures.modules.nested.greeting.Greeter { prefix = "full box: " }
    seed.io.show(short_box.message("ok"))
    seed.io.show(full_box.message("ok"))
    out none
}
